name: Pylint Django DRF Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  pylint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pylint django pylint-django djangorestframework

      - name: Run pylint with Django settings
        env:
          DJANGO_SETTINGS_MODULE: drf_lib.drf_lib.settings
        run: |
          pylint --load-plugins pylint_django drf_lib
